#include "main.h"

/**
 * _abs - computes absolute integer value
 * @n: program that computes the absolute value of an integer
 *
 * Return: value
 */
int _abs(int)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0);
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
