#include <stdio.h>

/**
 * main - printf runs the codes
 (*
 *Description: void allows the codes to run)?
 * Return: 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
